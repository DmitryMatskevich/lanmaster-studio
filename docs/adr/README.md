# Architecture Decision Records

ADR files owned by `lanmaster-studio` live here.

P0-05 will add the initial ADR set after the current CAD contracts, pilot
constraints, and ownership boundaries are confirmed. Decisions owned by
`lanmaster-cad` should be recorded in that repository instead.
